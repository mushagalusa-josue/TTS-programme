services:
  - type: web
    name: kokoro-tts-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_CMD
        value: python
    healthCheckPath: /health
    plan: free

